# Notes

- No trusted helpers were added.
- The bridge is intentionally simulated. Real ICRC post-await restore behavior
  is researched in 103-B.
- The account ledger book still relies on `BMap` ordering lemmas. The local
  kernel keeps receipt semantics separate from storage mechanics.

